// Placeholder — beui.dev's own value wasn't fetchable (network-blocked) when
// this component was stashed. This is a standard "strong ease-out" curve
// (matches the emil-design-eng / better-ui skills' recommended UI easing);
// swap in beui's exact export if it differs.
export const EASE_OUT = [0.23, 1, 0.32, 1] as const;
